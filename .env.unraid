# X32 Scene Manager - Unraid Environment Configuration
# =====================================================
# Copy this file to the same directory as your compose.yaml and rename to .env
# Path: /boot/config/plugins/compose.manager/projects/x32-scene-manager/.env

# Web server port (default: 3000)
PORT=3000

# X32 Mixer IP Address
# ---------------------
# Leave empty for auto-discovery (recommended with host network mode)
# Set to your X32's IP if using bridge network or auto-discovery fails
# Find your X32's IP: Mixer Setup → Network → IP Address
X32_IP=

# X32 OSC Port (default: 10023)
# Rarely needs to be changed
X32_PORT=10023

# Mock Mode
# ---------
# false = Connect to real X32 (production)
# true  = Use simulated X32 data (testing without hardware)
MOCK_MODE=false
